#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t,a,b;
    cin>>t;
    while (t--)
    {
        cin>>a>>b;
        int c[a],d[b];
        for (int i = 0; i < a; i++)
        {
            cin>>c[i];
        }
        for (int i = 0; i < b; i++)
        {
            cin>>d[i];
        }
        
    }
    
return 0;
}